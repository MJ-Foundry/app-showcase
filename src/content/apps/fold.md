---
title: Fold
tagline: Fold to find the shape.
description: A quiet paper folding puzzle — fold the sheet over or under itself until what is left matches the shape you are shown.
icon: /icons/fold.png
accent: ['#1e6f6a', '#5aa39d']
platforms: ['Android']
tech: ['TypeScript', 'Phaser 3', 'Vite', 'Capacitor']
tags: ['game', 'puzzle', 'origami']
status: 'Coming Soon'
featured: false
order: 3
updated: 2026-09-10
features:
  - 'Chapters of levels, each chapter teaching a new way the paper behaves'
  - 'Fold over or under, which decides whether a colour ends up hidden or on top'
  - 'Drag the paper across a crease with a finger and watch the flap turn over'
  - 'Every level has a proven minimum number of folds, and a star for reaching it'
  - 'Holes let you see the layers underneath, so the stack can be read from above'
  - 'Undo any fold, or start the sheet over, at any time'
  - 'Follows your phone between light and dark'
  - 'No timer, no score, no account — your progress is saved on the phone'
related: ['trigon', 'repeatio']
---

You are shown a shape and given a sheet of coloured paper. Drag the paper across
a crease and it folds — the flap turns over, lands on the other side and takes
its colours with it. Keep folding until what lies on the table matches the shape.

A fold is never only a move. The paper is mirrored, so a shape flips handedness;
it is moved, so it covers something else; and it is stacked, either over or
under what it lands on. Choosing over or under is the whole game in the later
chapters, because it decides which colour ends up facing you. Holes are what let
you see that at all: two holes stacked on top of each other become a window, and
a hole over a colour shows that colour through the sheet.

The chapters start with a single crease and end with sheets folded deep
enough that the order of the layers is the puzzle. Every level was checked by a
solver that searches all fold sequences, so the fewest possible folds is a
proven number rather than a guess — and solving a level in exactly that many
earns a star.

Nothing is timed and nothing is scored. The paper rustles when it settles, the
phone gives a short tap, and that is the extent of the noise.
